#include "header.h"

/*
 pthread_attr_t attr;
    size_t stack_size;

    pthread_attr_init(&attr);
    if (pthread_attr_getstacksize(&attr, &stack_size) == 0) {
        std::cout << "默认线程栈大小: " << stack_size << " 字节" << std::endl;
    }
    pthread_attr_destroy(&attr);
*/
#    include <immintrin.h>
int main() {
  return 0;
}